---

dependencies: []

galaxy_info:

  author: 'Robin Schneider'
  description: 'Define a message which is shown at boot from the init script (initramfs).'
  license: 'AGPL-3.0'
  min_ansible_version: '1.3'

  platforms:

    - name: Debian
      versions:
      - all

      ## Not sure if this message would actually be visible?
    # - name: Ubuntu
      # versions:
      # - all

  galaxy_tags:
    - system
    - initramfs
    - boot
